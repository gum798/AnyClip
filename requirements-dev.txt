-r requirements.txt
pytest>=7.0
pytest-asyncio>=0.21
# Packaging tools used by the release CI. Markers keep `pip install`
# clean on the wrong OS.
py2app>=0.28; sys_platform == "darwin"
pyinstaller>=6.0; sys_platform == "win32"
